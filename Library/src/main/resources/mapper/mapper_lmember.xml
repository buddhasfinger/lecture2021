<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.library.mapper.LMember_Mapper">
    
    <select id="selectloginCheck"  resultType="com.library.dto.lMemberDto">
       select * from lmember where lm_id=#{lm_id} and lm_pw=#{lm_pw}
  	</select>
  
  </mapper>